import React from "react";
import Poke2 from "./poke2";

class Pokedex2 extends React.Component {
  
  render() {
  
    const { pokemons } = this.props;

    return (
      <div>
        {pokemons.map((pokemon) => (
        <Poke2
          key={Poke2.id}
          pokemon={Poke2}
        />
        ))}
      </div>
    );
  }
}

export default Pokedex2;